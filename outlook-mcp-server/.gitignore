.venv/
__pycache__/
*.py[cod]
*.egg-info/
.pytest_cache/
.mypy_cache/
# Ignore every local env file (incl. .bak/.local copies that hold live tokens),
# but keep the committed template.
.env*
!.env.example
dist/
build/
